import React from 'react';

// Skeleton loading disabled - returns null to remove loading animations
const Skeleton = ({ className='' }) => null;

export const SkeletonText = ({ lines=3 }) => null;

export default Skeleton;
