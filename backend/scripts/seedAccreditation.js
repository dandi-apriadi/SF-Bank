import dotenv from 'dotenv';
dotenv.config();

// Accreditation seeder archived: study program concepts removed for SF BANK.
// This script is kept for reference but will not perform seeding in the new project.
console.log('seedAccreditation.js is archived: study program concepts removed for SF BANK.');
